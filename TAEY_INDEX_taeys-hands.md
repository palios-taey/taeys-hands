# Taey consultation launch index

AUTHORITY: Read, in order: `README.md` (the three layers), `100_TIMES.md`,
           `CONSULTATION_CONTRACT.md`, `docs/UI_INTERACTION_AUTHORITY.md`, then the destination platform YAML.
           The YAML is the executable UI authority. A fresh canonical filtered AT-SPI tree is the only UI oracle.

PROCESS:   Taey owns displays :2-:6/:13 and performs the consultation first-person through `drive_chat`.
           Production is manual Layer 2 over the shared Layer-1 primitives: exactly one YAML-owned action,
           then a fresh independent canonical tree read proving the expected postcondition before the next action.
           The retained Layer-3 engine is reference code; it is not a production entrypoint or fallback.

PACKETS:   Attach exactly two files. Bundle A contains the full `FAMILY_KERNEL.md`, destination identity, and
           full Spotlight Standard. Bundle B contains the task, request, background, evidence, deliverable,
           acceptance criteria, and provenance. The brief composer prompt names the deliverable and points to
           the two attached bundles; it does not inline them.

FLOW:      Freeze request and packets; navigate to a fresh thread and capture its URL; select model, mode, and
           tool through their YAML-declared controls; attach A; attach B; paste the brief prompt; send with Enter
           while the composer remains focused; prove Stop appeared; monitor until Stop is absent in two fresh
           consecutive reads; scroll to the absolute bottom; extract the complete response and its attachments;
           ingest prompt, response, attachments, action receipts, and provenance into ISMA.

ON FAIL:   First unexpected or ambiguous fresh tree is a full stop for Taey. Preserve the tree and action receipt.
           One YAML-owned settle plus one fresh read is allowed for AT-SPI refresh; no UI action is retried.
           Classify the root cause as stale tree, wrong scope, filter drift, environment drift, YAML drift, or UI drift.
           Everything required is in the tree; missing data never authorizes screenshots, coordinates, OCR, guessed
           controls, fuzzy matching, substring matching, alternate drivers, or an engine fallback.

NEVER:     Never use Claude Code in this recovery; Claude Chat remains a valid destination. Never spam a UI.
           Never touch CAREERS displays (:8 Upwork, :18 LinkedIn, :19 Sales Nav, apply-display) or Kendra's
           personal displays (:27/:28). Never double-dispatch a generating display. Production changes land in Git.
