# Response extraction patterns

Status: current operating reference. The destination platform YAML is the executable authority.

## Common lifecycle

Extraction begins only after the passive monitor has observed Stop disappear in two consecutive fresh reads.
Scroll to the absolute bottom, resolve the YAML-mapped response Copy control from a fresh canonical tree, and
select the last mapped Copy control when the page contains more than one. Activate it through the declared
operation, then read the clipboard. For the current ordinary-response contract, bottom-of-thread plus the last
exact mapped Copy control is authoritative; do not insert a separate speaker-attribution heuristic. Never select
the control from guessed text, browser chrome, raw coordinates, pixels, OCR, or a stale tree.

The returned body must be non-empty and must differ from the submitted brief. Record its byte count and SHA-256
from the extracted file, not from model-authored prose. If the platform declares response attachments, handle
them only through that platform's exact YAML workflow and preserve separate file receipts.

## Platform ownership

- ChatGPT: use its `extraction:` declaration; research or Canvas artifacts require their own mapped artifact
  controls and may not be replaced with the surrounding summary.
- Claude Chat: use the last mapped response Copy control; artifact handling is separate from the narration.
- Gemini: normal responses and Deep Research reports use distinct YAML-declared extraction controls.
- Grok: use the last mapped response Copy control after scrolling to bottom. Grok Imagine is a separate surface
  and is outside this consultation contract.
- Perplexity: a report card's mapped `Copy contents` control takes precedence over a summary Copy control when
  the YAML declares a research report.

If the exact control or declared postcondition is missing or ambiguous, stop and preserve the fresh tree. Do not
substitute a different button or introduce a fallback in prose.
