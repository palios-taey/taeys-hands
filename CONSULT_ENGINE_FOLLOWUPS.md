# Project: consult-engine-followups - Consult Engine Post-Migration Follow-ups
> Post-migration hardening surfaced during the platform-independence migration (now 6/6 COMPLETE, retire merged 1e45c60b 2026-07-05). ACTIVE decommission-phase work — each a real gap/hardening. Assigned to taeys-hands-codex (code), Fable specs/gates/vals. Priority low-number-first.

## Phase: followups - Deferred fixes + hardening [order: 1]

### Task: fu-claude-artifact-extract - Map the live Claude right-panel artifact Copy control [priority: 20] [owner: taeys-hands-codex]
- pkg-claude val proved `extract_additional` fails: "no configured control yielded artifact content". Claude puts the real deliverable in a right-panel ARTIFACT; the package's mapped artifact_copy_keys + AT-SPI candidates don't resolve the live Copy control. Confirmed PRE-EXISTING (main's blind-coord helper was condemned HIGH#8, removed w2d) — behavior-parity, so it merged; but artifact-bearing consults lose the artifact (prose extract_primary works). FIX: map the live right-panel artifact Copy control (same class as the perplexity `Copy contents`→`Copy` UI-drift). Verify with a real artifact-producing Claude consult.

### Task: fu-perplexity-export-markdown-fallback - Add Export-as-Markdown 0-control extract fallback [priority: 20] [owner: taeys-hands-codex]
- Perplexity UI drifted: the extractor's mapped `copy_contents_button` ("Copy contents") is GONE; the answer copy is now a plain `Copy` push button (val-retire 2026-07-05 confirmed the report-card control absent, per-answer copy icon at x776 worked), and some DR layouts expose neither mapped control → extract_primary fails "no mapped extraction control present". Manual recovery that works: the response `...` menu → "Export as Markdown" → full-content download, OR the per-answer copy icon. FIX: (a) remap the answer-extract to the current plain `Copy`/per-answer-copy control; (b) add "Export as Markdown" (via `...` menu) as a 3rd fallback extract for the 0-control DR-report case.

### Task: fu-gemini-dr-start-research-send-gap - Fix Gemini DR "Start research" click not firing on send [priority: 20] [owner: taeys-hands-codex]
- doge-runner 2026-07-05: on Gemini Deep Research, the engine generates the DR plan and empties the composer but FAILS to click the "Start research" button (the actual DR launch) → the DR never launches → the engine process exits at send-fail with no answer. Recovered manually via grab_focus + do_action on the "Start research" push button (screenshot-located). FIX: the gemini package send path must locate + click "Start research" reliably (focus composer first + wait for the button to enable, then do_action on the mapped element) so DR launches without manual intervention.

### Task: fu-gemini-profile-aware-element-names - Profile-aware Gemini element names for 2-set parallelism [priority: 30] [owner: taeys-hands-codex]
- The 2nd Gemini display-set profile (`ff-profile-mira-gemini-2`, :22, "Ultra" account) has a DIFFERENT model menu ("3.1 Pro"/"3.5 Thinking", NO "Deep Think") than the 1st profile — gemini.yaml element names don't match → select fails on :22, forcing all gemini lanes to :4. FIX: profile-aware element names (or a 2nd-profile YAML variant) so gemini runs on both display sets for full 2-set parallelism.

### Task: fu-routing-parent-pkg-error-translation - Fix routing.py error-translation for a missing PARENT pkg [priority: 40] [owner: taeys-hands-codex]
- routing.py:16-18 error-translation is broken for a missing PARENT package (exc.name is the parent → the teaching-RuntimeError branch is skipped, raw ModuleNotFoundError propagates). Still fail-loud + unreachable from prod, so cheap. FIX: handle the parent-pkg case in the error translation.

### Task: fu-osresource-find-firefox-race - Per-package find-Firefox window/bus concurrency assertion [priority: 40] [owner: taeys-hands-codex]
- The shared `_routing_core` + AT-SPI/DBus bus + Firefox process tree are OS-level shared-mutable resources; per-package binding is CONFIG-level only (dedicated DISPLAY + PID file). Runtime serialization comes from the display-keyed dispatch lock (5de5c6dd), not the routing layer — lint-clean ≠ runtime-race-free under concurrent/rapid dispatch. FIX: add a per-package concurrency assertion that each find-Firefox binds its OWN window/bus (no cross-bind), exercised by concurrent/rapid-sequential dispatch across ≥2 packages.

### Task: fu-gemini-atspi-select-hardening - Fix Gemini model-picker select (stale menu_snapshot + Ultra menu names) [priority: 15] [owner: taeys-hands-codex]
- Gemini's automated model/mode SELECT genuinely FAILS (this session: fsdp_gemini fails=['select']). ROOT CAUSE (isolated by driving gemini MANUALLY 2026-07-07, feedback_gemini_mode_picker_and_mic_doorhanger): after the model menu OPENS cleanly, `menu_snapshot()` reads a STALE AT-SPI tree so the structural-match can't find the mode option — DESPITE the option EXISTING and a MANUAL click working. TWO parts: (1) STALE-READ — add a post-open SETTLE + bounded RE-SNAPSHOT retry loop on the mode_picker (re-read menu_snapshot until the target option's structural match stabilizes/appears, with a timeout). (2) ULTRA MENU NAMES — the gemini.yaml maps 'Deep Think'/mode_pro names that DO NOT EXIST on the Ultra account; the live menu is exactly '3.5 Flash' (all-around) / '3.5 Thinking' (solves complex problems = the deep/reasoning mode) / '3.1 Pro' (advanced math and code). Map these EXACT names from a live AT-SPI scan (THE RULE — exact match, no fuzzy). GATE-BRANCH (conductor CONTROL call): FIRST determine gemini-specific vs shared-leaf — does the stale-read reproduce on OTHER platforms' menu_snapshot reads, or ONLY gemini's mode_picker? (A) package-local (PREFERRED — the settle+re-snapshot lives in gemini/driver.py + the names in gemini.yaml, preserves isolation) → owner-verdict + live-proof, taeys-hands stream. (B) ONLY IF the stale-read is in the SHARED menu_snapshot LEAF primitive affecting all 5 packages → INDEPENDENT r5. PROOF: a real gemini consult that AUTO-SELECTS the mode (no manual click) + validates the selection + completes e2e. (Separate NOTE, not this fix: gemini 3.5-Thinking server-side mode was HANGING on the Ultra account 2026-07-07 — 2 clean sends stuck blank >14min; Pro works. That's a gemini-server issue; if it persists, package could prefer Pro or add a Thinking-generation timeout+fallback.)

### Task: fu-concurrent-launch-timing - Two select/send failures under 5-way concurrent-launch panel load [priority: 30] [owner: taeys-hands-codex]
- Surfaced live by the WEDGE R3 5-platform panel (2026-07-07, dispatched staggered 80s apart). BOTH failed under concurrent-launch load and BOTH worked when re-run solo — so both are LOAD-induced timing, not logic bugs. (a) GEMINI SELECT: 'gemini selection expected element mode_pro missing after menu open' — the f89831e3 1000ms post-open settle is INSUFFICIENT when 4 other engines are launching Firefox/AT-SPI concurrently; solo the same consult selects fine. (b) CLAUDE SEND-VALIDATION FALSE-NEGATIVE: RESULT fails=['send'] 'user message did not land in thread' but the send LANDED (thread created + titled + generated a full artifact — salvaged manually via panel Copy). The send-landed-false-negative (thought fixed ab87a204) RECURS under concurrent panel load — the post-send validation probe reads a stale/contended tree and concludes not-landed, then the engine dies leaving a live generating browser. ROOT-CAUSE DIRECTION (measure first via gitnexus): the failing reads (menu_snapshot after mode-open; send-confirm probe) need to be resilient to AT-SPI tree contention under concurrent Firefox load — either (i) scale the settle/retry budget when concurrent load is detectable, or (ii) make the panel stagger adaptive/wider (80s insufficient for 5-way), or (iii) make the send-confirm probe poll longer before declaring not-landed (it currently false-negatives a landed send). PREFER the read-resilience fix (settle/poll scaling) over just widening stagger — widening stagger only hides it. NON-BLOCKING: the panel completed via manual salvage; this hardens concurrent panels so manual salvage isn't needed. PROOF: a 5-way concurrent panel where all 5 select + send-confirm without a false-fail.

### Task: fu-grok-newchat-timeout - Grok new_chat setup times out (>20s) under concurrent-launch load [priority: 35] [owner: taeys-hands-codex]
- Surfaced by codex's fu-concurrent-launch-timing validation panel (2026-07-07): with the gemini-select + claude-send-confirm fixes proven, a full 5/5 concurrent proof STILL failed because GROK failed BEFORE select with `TimeoutError: setup step 'new_chat' exceeded 20.0s` (/tmp/consult_pkgs/fu_concurrent_r3_grok.log) under concurrent 5-way Firefox/AT-SPI load. This is a SEPARATE adjacent bottleneck from the two just-fixed findings — grok's new_chat setup step (the fixed 20s budget) is insufficient when 4 other engines are launching concurrently; solo grok's new_chat completes fine. ROOT-CAUSE DIRECTION (measure via gitnexus first): the new_chat setup step needs a load-resilient budget/retry (same class as the gemini-select settle-scaling just merged 0edc5b02) — scale the 20s timeout under concurrent load OR make new_chat poll-until-ready with a wider ceiling rather than a hard 20s wall. Do NOT just widen the panel stagger (hides it). PROOF: a 5-way concurrent panel where grok's new_chat completes + grok reaches select+send without timeout — i.e. the full all-five concurrent proof that fu-concurrent-launch-timing could not reach because of this. NON-BLOCKING: staggered/solo grok works; this closes the last gap to a clean 5/5 concurrent panel.

### Task: fu-chatgpt-clean-composer-false-fail - ChatGPT clean_composer verification false-fails on a genuinely-clean composer [priority: 30] [owner: taeys-hands-codex]
- Surfaced by the scoring/funnel audit panel (2026-07-08). ChatGPT lane failed at setup step `clean_composer` ('ChatGPT clean composer verification failed') — steps reached only navigate→clean_composer, nothing sent. Failed BOTH under concurrent load AND on a SOLO re-run. SCREENSHOT-VERIFIED the composer was genuinely CLEAN both times: empty 'Ask anything' placeholder, fresh 'Where should we begin?' screen, Pro Extended selected, no leftover draft. So the clean_composer VERIFICATION itself false-fails on a clean composer — same read-resilience/over-strict-probe class as the gemini-select (f89831e3) and claude-send-confirm (0edc5b02) fixes, but on chatgpt's clean_composer setup probe (untouched by those). RECOVERY USED: drove chatgpt MANUALLY (paste prompt+packet inline into the verified-clean composer, Pro Extended, send → thread generated fine) — so the composer works; only the automated verification gate is broken. ROOT-CAUSE DIRECTION (gitnexus first): the clean_composer probe reads the composer as not-clean when it IS clean — likely an AT-SPI read of the composer subtree that mis-detects placeholder/empty state, or a stale/contended read. Make it resilient (poll/settle, or fix the empty-detection predicate against the live 'Ask anything' placeholder DOM). PROOF: a chatgpt consult (solo AND in a concurrent panel) where clean_composer passes on a genuinely-empty composer + full send/extract completes. Package-local chatgpt. NON-BLOCKING: manual drive works; this removes the need for manual salvage on chatgpt panels.

### Task: fu-chatgpt-clean-composer-yaml-prune - ROOT CAUSE FOUND: clean_composer false-fails on 4 unpruned decorative composer chips [priority: 10] [owner: taeys-hands-codex]
- SUPERSEDES the vague fu-chatgpt-clean-composer-false-fail. ROOT CAUSE measured live 2026-07-09 (DISPLAY=:20 fresh composer, ran the actual `_missing_expected_elements` + `_conformance_unknown_discrepancies` + `_selection_base_snapshot_clean` predicates the clean_composer step uses): MISSING expected = [] (all YAML-expected base elements present, input name 'Chat with ChatGPT' is CORRECT). The failure is entirely UNKNOWN DISCREPANCIES — 4 elements in the LIVE tree that are NOT in chatgpt.yaml's base-scope conformance known/pruned list, so `_conformance_unknown_discrepancies` returns them and `_selection_base_snapshot_clean` → False → clean_composer fails on EVERY chatgpt engine consult: (1) tool tip 'Add files and more @', (2) push button 'Create an image', (3) push button 'Write or edit', (4) push button 'Look something up'. These are ChatGPT's DECORATIVE quick-action suggestion chips + attach tooltip on a fresh empty composer — added since the YAML's 'Verified live 2026-06-21' snapshot, non-critical, NOT part of the composer contract. FIX (Jesse-directed 2026-07-09: 'that element is not needed so should be filtered out'): add these 4 to chatgpt.yaml's base-scope conformance PRUNE/IGNORE list (same mechanism that already prunes the 13 sidebar keys — see chatgpt.yaml ~line 40-64 base scope comment), so conformance stops flagging them as unknown. Root-cause YAML update, NOT a driver patch. VERIFY IT'S A YAML EXACT-MATCH (THE RULE): prune by the exact live names/roles above. PROOF: a real chatgpt engine consult (solo AND concurrent) where clean_composer PASSES + full send/extract completes — no manual drive needed. Package-local chatgpt. gitnexus the conformance prune path first. This is a STALE-YAML class bug — audit the OTHER platforms' conformance scopes for the same drift (elements added since their last live-verified date).

## User Stop Conditions
- stop_when_all_ready_tasks_dispatched

### Task: fu-display-render-parity-and-concurrent-churn - Displays render different composers (:2 vs :20) + concurrent-firing churns clean_composer [priority: 25] [owner: taeys-hands-codex]
- Found while validating the clean_composer YAML-prune fix (2026-07-09). TWO related findings: (A) DISPLAY RENDER PARITY: after the chip-prune fix, a fresh chatgpt composer on :20 (1920x1080) is CLEAN (MISSING=[], UNKNOWN=[]) but on :2 (1920x1063) shows MISSING=[model_selector, dictation, voice_button] — the SAME ChatGPT/account renders a different composer toolbar on the two displays. Codex's solo proof passed (:20-class), the primary :2 display intermittently doesn't render the toolbar on a fresh navigate → clean_composer MISSING-fail. Likely display geometry/zoom/viewport difference (:2 is 17px shorter) OR :2 was in a churned state. NEEDS: characterize whether it's geometry (responsive UI hiding the toolbar at :2's size) or render-timing (toolbar loads late; the engine's clean-wait loop should poll longer) — MEASURE :2 fresh-composer render over time + compare geometry, then either fix :2's display geometry to match :20 or make the clean-wait resilient to late toolbar render. (B) OPERATOR CHURN (taeys-hands self-lesson, NOT codex): firing multiple chatgpt engine runs back-to-back on the same display churns it so clean_composer never sees a clean state — I did this 4x, making a merged-and-proven fix look broken. RULE: ONE consult per display at a time; on a failure, kill ALL procs + let the display settle + scan conformance BEFORE re-running; never rapid-re-fire. PROOF for (A): fresh chatgpt consult on :2 passes clean_composer reliably (no churn, settled display). Package-local chatgpt + display-config.
