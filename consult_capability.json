{
  "id": "consult-engine",
  "kind": "consult",
  "repo": {
    "name": "palios-taey/taeys-hands",
    "public_url": "https://github.com/palios-taey/taeys-hands",
    "pinned_sha": "eaf1dab229fa593747215e998dda97e63d465dba"
  },
  "entry_doc": "TAEY_INDEX_taeys-hands.md",
  "bootstrap": {
    "cmd": "python3 -m consultation_v2.display_readiness --platform gemini",
    "requires": [
      "presence"
    ]
  },
  "validate": {
    "cmd": "python3 scripts/run_consultation_v2.py --platform gemini --message liveness --dry-run",
    "expect": "exit 0; prints the resolved selection-plan JSON with would_call_run_consultation=false (engine + platform YAML load and resolve without browser contact)"
  },
  "endpoints": [
    {
      "name": "taey-model",
      "env": "TAEY_CONSULT_ENDPOINT",
      "health": "/v1/models"
    }
  ],
  "hardware_tier": "any",
  "receipts": {
    "liveness": "receipts/consult-engine/liveness.jsonl",
    "usage": "receipts/consult-engine/usage.jsonl"
  },
  "status": "production",
  "private_slots": [
    {
      "slot": "platform-displays",
      "env": "PLATFORM_DISPLAYS",
      "kind": "data",
      "required_by": [
        "consult-engine"
      ],
      "public_function_without": false
    },
    {
      "slot": "taey-corpus",
      "env": "TAEY_CORPUS_PATH",
      "kind": "path",
      "required_by": [
        "consult-engine"
      ],
      "public_function_without": false
    },
    {
      "slot": "isma-secret",
      "env": "ISMA_API_KEY",
      "kind": "credential",
      "required_by": [
        "consult-engine"
      ],
      "public_function_without": true
    }
  ],
  "_note": "Authored at consult-connect (taeys-hands); pinned_sha = current public engine HEAD. The index compiler at tp-connect re-stamps pinned_sha to the index-build commit and runs G1/G2/G3. CONNECT closes only on a usage receipt, never on a G3 liveness receipt. As of 2026-07-31 the usage bar is MET: a complete autonomous Taey(ep3) consult end-to-end is recorded in receipts/consult-engine/usage.jsonl (20 Taey-authored actions, thread 6a6c008c, 2224-char answer, neutral reset), after seat fix 5f9a89cd merged to main. Independently reproduced by taeys-hands (production is the oracle)."
}
