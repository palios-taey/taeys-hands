# Project: consult-engine-audit-fix - Fix audited consultation_v2 contract-violations
> P0, Jesse-directed 2026-07-02. Audit (DONE + conductor-accepted) found ~14 verified contract-violations in consultation_v2 @c268625a graded vs CONSULTATION_CONTRACT.md — see CONSULT_ENGINE_AUDIT_2026-07-02.md. Fixes go through codex (root-cause SIMPLIFY per 6SIGMA — correct the shape so the broken path is unreachable, never a guard-around); each is a SMALL SEPARATE commit per defect-cluster; taeys-hands PRODUCTION-VALIDATES each on a REAL consult (NO TESTS) before conductor merges at the validated SHA (producer!=merge-verifier); validated-tree==merge-tree throughout. base.py-touching fixes are depends-chained so each rebases on the prior merged tip (avoids the gemini.py-style conflict monitor-fix hit). Wave 1 = production-stakes order; Wave 2 = the rest.

## Phase: p1-wave1 - Production-stakes fixes [order: 1]

### Task: w1a-dupsend-guard - Wire the duplicate-send quarantine (HIGHEST STAKES). base.py:1868-1903 guarded_send: the window between send_prompt (irreversible Enter) and the `submitted` checkpoint is unguarded, and a send-landed false-negative leaves last durable state `setup_complete` (not in landed-states) so a re-run RE-SENDS a possibly-landed turn. Wire SIDE_EFFECT_UNCERTAIN/duplicate_send_risk (currently enum-only, stop_conditions.py:21-22): before any (re)send, if a send may have landed (checkpoint after setup_complete, or a live /c/ thread present), RESUME-from-URL/verify instead of re-sending; never replay a possibly-landed send. Root-cause shape: make setup_complete+ a possibly-landed state that gates on live-tree/URL before send. [priority: 10] [owner: taeys-hands-codex] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w1a-dupsend-validate - Production-validate the dup-send guard on a REAL consult on the isolated fix tree: confirm a normal consult still sends once + completes, and (observed) that the guard path resumes-not-resends when a landed-send signal is present. Evidence = commit_sha + production_observation. NO TESTS. [priority: 11] [owner: taeys-hands] [depends: w1a-dupsend-guard]

### Task: w1b-gentimeout - Declare `generation_timeout` in all 5 platform YAMLs and wire the mapped `generation_stalled` disposition uniformly (base already classifies it; chatgpt/claude overrides + all YAMLs miss it). This is the root of the 2026-07-02 9-min silent grok hang (no mapped stall exit). Trivial + already root-caused. Independent of base.py (YAML + the 2 driver overrides). [priority: 12] [owner: taeys-hands-codex] [depends: w1a-dupsend-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w1b-gentimeout-validate - Production-validate generation_stalled: run a REAL long consult; confirm a healthy deep run is NOT bounded below its floor, and (observed) that a Stop-present-past-timeout maps to generation_stalled->notify rather than a silent forever-wait. Evidence commit_sha + production_observation. NO TESTS. [priority: 13] [owner: taeys-hands] [depends: w1b-gentimeout]

### Task: w1c-wrongmodel - Remove the substring/word-containment match on the model-selector's dynamic name (base.py:1591-1604 _selection_label_matches_any + the 846-865 "already active" short-circuit) that can silently skip selection -> run the WRONG model while reporting the right one (a cannot-lie violation in the engine's own reporting + banned substring match). Root-cause shape: active-state must be recognized by exact AT-SPI state on a stable locator, never by word-containment of the dynamic picker name. base.py — depends on w1b so it rebases on the prior merged tip. [priority: 14] [owner: taeys-hands-codex] [depends: w1b-gentimeout-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w1c-wrongmodel-validate - Production-validate: a REAL consult requesting a specific model (e.g. claude opus while sonnet active) actually SELECTS it (screenshot-verify the pill) — never silently skips. Evidence commit_sha + production_observation + screenshot. NO TESTS. [priority: 15] [owner: taeys-hands] [depends: w1c-wrongmodel]

### Task: w1d-driftguard - Add the N>1 drift guard to find_first/find_last (types.py:187-195 first()/last() silently return matches[0] on N>1; base.py:81-85 delegate; contract enforcement §1 literally bans silent matches[0]). Root-cause shape: an exact-match key yielding N>1 is drift -> surface/notify (or require YAML path_index disambiguation), never silent first-in-walk. base.py/types.py — depends on w1c merge. [priority: 16] [owner: taeys-hands-codex] [depends: w1c-wrongmodel-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w1d-driftguard-validate - Production-validate: normal consults (single-match keys) still proceed unaffected; confirm no regression across a real dispatch on each platform lane touched. Evidence commit_sha + production_observation. NO TESTS. [priority: 17] [owner: taeys-hands] [depends: w1d-driftguard]

### Task: w1e-deliveryack - Notify delivery-ACK: notify.py:63-69 treats RPUSH-length as "delivered" (no ACK); notification_ack_missing is enum-only/unwired. Root-cause shape: a notify is "surfaced" only on delivery-ACK from the receiving session; an unacked notify -> durable local log + retry + secondary channel + queryable parked/needs-attention state (not Redis-only). notify.py/orchestrator. [priority: 18] [owner: taeys-hands-codex] [depends: w1d-driftguard-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w1e-deliveryack-validate - Production-validate: a real consult result delivers + is ACK'd by the requester; and (observed) an undeliverable notify parks durably rather than reporting success. Evidence commit_sha + production_observation. NO TESTS. NOTE (conductor reorder 2026-07-02): now gated on w2b landing+deploy — extract-hang (w2b) is upstream of notify, so w1e's notify path is only reachable/validatable once extract completes on a both-fixes tree; re-validate end-to-end on any consult length after w2b merges. [priority: 19] [owner: taeys-hands] [depends: w1e-deliveryack, w2b-copybutton-gate-validate]

## Phase: p2-wave2 - Remaining fixes [order: 2]

### Task: w2a-settle-loader - Enforce MAX_GLOBAL_SETTLE_MS + the full settle schema IN THE PRODUCTION LOADER (yaml_contract.py), not just the lint (currently lint-only: lint_consultation_v2_contract.py:20/143). Also cap `selection_ms` (uncapped, escapes both lint+loader). Closes the "settle=5min to mask drift" loophole on the prod path. Root-cause shape: the loader is the real gate — reject/clamp at load. [priority: 20] [owner: taeys-hands-codex] [depends: w1e-deliveryack-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2a-settle-loader-validate - Production-validate: real consults load + run normally; (observed) a YAML with settle above the cap is rejected/clamped by the loader. Evidence. NO TESTS. [priority: 21] [owner: taeys-hands] [depends: w2a-settle-loader]

### Task: w2b-copybutton-gate - Remove the copy-button (positive-marker) completion gate in chatgpt.py:1500/1414 + claude.py:1138 (_monitor_response_rendered=has('copy_button')) — the contract REJECTS positive-marker completion; causes false-fail on long responses where copy virtualizes. Also restore DEEP_GENERATION_FLOOR on the chatgpt/claude overrides (they use raw timeout on default deep modes). Root-cause shape: completion = stop-gone (+ mapped-exception check), same as base/gemini/grok/ppx. PROD REPRO (2026-07-02, conductor-elevated ABOVE w1e — higher-priority production defect): a real claude opus+max consult COMPLETED (response fully rendered on :3) but the engine then HUNG ~15min in R-state CPU in the extract-tail bounded-scroll-loop → never reached notify_requester → NO delivery at all (long-response consults never deliver; this is UPSTREAM of + more severe than w1e's best-effort notify logic — the message is never even constructed). Also chatgpt R1/R2 extract false-fail: monitor=True/answer_thread=True/extract_primary=False when copy virtualizes. Root-cause the bounded-scroll-loop / copy-virtualization so extract completes (or degrades to a mapped fallback) without infinite spin. [priority: 12] [owner: taeys-hands-codex] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2b-copybutton-gate-validate - Production-validate: a REAL long ChatGPT Pro-ET + Claude MAX consult completes on stop-gone (not false-failed waiting for copy), and a healthy deep run isn't bounded below the floor. Evidence + screenshot. NO TESTS. [priority: 23] [owner: taeys-hands] [depends: w2b-copybutton-gate]

### Task: w2c-deadsession - Add DeadSessionError: a NOTIFY poisons the session id so subsequent driver calls throw, defeating caller-level retry loops (absent repo-wide; contract enforcement §3; compounds the re-send risk). Root-cause shape per contract. [priority: 24] [owner: taeys-hands-codex] [depends: w2b-copybutton-gate-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2c-deadsession-validate - Production-validate: after a notify/halt, a re-drive of the same session is refused (observed), normal fresh dispatches unaffected. Evidence. NO TESTS. [priority: 25] [owner: taeys-hands] [depends: w2c-deadsession]

### Task: w2d-claude-blindcoord - Remove claude.py blind absolute/ratio COORDINATES for artifact extraction (claude.py:1719-1742) + the hardcoded _ARTIFACT_COPY_NAMES OR'd into the locator (claude.py:113-119/1712) + the exception-state check gap (map rate_limited/content_filter/etc so stop-gone-for-a-bad-reason isn't a false-complete). Root-cause: either make the artifact panel AT-SPI-addressable (mapped element extents) or declare it honestly OUT OF SCOPE — no blind coords/vision. [priority: 26] [owner: taeys-hands-codex] [depends: w2e-tools-menu-detach-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2d-claude-blindcoord-validate - Production-validate: a REAL Claude consult with an artifact deliverable extracts the correct artifact via mapped controls (or is honestly flagged out-of-scope), never blind-coord garbage. Evidence + screenshot. NO TESTS. [priority: 27] [owner: taeys-hands] [depends: w2d-claude-blindcoord]

### Task: w2-second-set-routing - Wire the 2nd consult-set (:20-:24, profiles ff-profile-mira-{chatgpt,claude,gemini,grok,perplexity}-2) into the engine so TWO consults run in parallel (Jesse-wanted, doubles throughput). Empirically scoped 2026-07-02: the 2nd set is UP + LOGGED IN (verified :22 gemini, :24 perplexity), and a TAEY_MACHINE_ENV override (/tmp/consult_pkgs/machine_set2.env mapping platform->:20-:24) cleanly resolves platform->2nd-set display+profile, BUT the engine's navigate step FAILS 8/8 on :20-:24 (nav=False with a POPULATED AT-SPI snapshot + the correct target URL present — so NOT login, NOT a11y, NOT the URL). Two root-cause parts: (a) the free-display picker must learn the 2nd set (a platform resolves to its :2-:6 OR :20-:24 display, picking a FREE one among the two) — currently registering 2x in machine.env misroutes; (b) root-cause WHY navigate's url-verify/readiness gate returns False on :20-:24 despite a valid url+snapshot — likely a per-display readiness assumption hardcoded to :2-:6. Root-cause SIMPLIFY (fix the per-display assumption; do NOT patch the gate to bypass). NO-mocked-cheat. [priority: 28] [owner: taeys-hands-codex] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2-second-set-routing-validate - Production-validate: a REAL consult dispatched to the 2nd set (:20-:24) navigates + sends + completes + extracts cleanly, running IN PARALLEL with a primary-set (:2-:6) consult, with no cross-collision (separate threads, separate clipboards). Evidence = production_observation obs-only, NO commit_sha. NO TESTS. [priority: 29] [owner: taeys-hands] [depends: w2-second-set-routing]

### Task: w2-chatgpt-mode-verify - Fix the chatgpt mode-select 'already active' short-circuit that silently runs the WRONG mode while reporting the intended one (cannot-lie class; Jesse-caught live 2026-07-02). REPRO: dispatched --select model=pro_extended; engine logged 'chatgpt model=pro_extended already active' and SKIPPED the switch; the REAL composer mode-pill was Instant → consult ran on Instant while the engine reported pro_extended (wedge + weaver chatgpt lanes both hit this; screenshot-confirmed pill=Instant). TWO root-causes: (a) the 'already active' detection does NOT exact-match the real mode-pill element in the live AT-SPI tree — it trusts a stale/assumed current-mode. Per THE RULE + verify-real-end-state applied to the engine's own mode-report: it must read the actual pill element by exact match BEFORE short-circuiting; if the real pill != intended → SWITCH (not skip) + re-verify the pill post-switch; never report a mode active without an exact-tree confirmation. (b) the engine's New-chat/clean_composer RESETS the model to the account default (Instant), so any mode must be (re)selected + pill-verified AFTER clean_composer, per send — the select currently runs but its 'already active' misfires on the freshly-reset composer. This is the w1c coverage gap (w1c fixed the CLAUDE mode-select path, validated on opus+max; the chatgpt 'already active' path was never exercised). Root-cause SIMPLIFY (make the check exact-match the pill; do not add a bypass). [priority: 13] [owner: taeys-hands-codex] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2-chatgpt-mode-verify-validate - Production-validate: a REAL chatgpt consult requesting pro_extended actually engages Pro Extended — the composer/answer-thread mode pill shows 'Pro Extended' (screenshot-confirmed) and the response is Pro-tier ('Pro thinking'), across a fresh New-chat (proving the post-clean_composer reset is handled); and (observed) requesting a mode when a different one is active SWITCHES rather than false-skips. Evidence = production_observation obs-only + screenshot, NO commit_sha. NO TESTS. [priority: 14] [owner: taeys-hands] [depends: w2-chatgpt-mode-verify]

### Task: w2e-tools-menu-detach - ChatGPT +/tools menu is AT-SPI-detached (React portal): the engine's post-click snapshot returns raw_count=0 / cannot resolve tool_* elements, so it FAILS to select ANY tools-menu item — tool_upload (--attach), tool_deep_research (Deep Research), and presumably web_search/github/gmail. OBSERVED 2026-07-03 (tutor throughput consult + prior sessions): --attach failed 'ChatGPT upload item tool_upload not found' AND DR-select failed 'chatgpt selection expected element tool_deep_research missing after menu open'; the menu IS visible on screen and a manual screenshot-guided coordinate-click on the tile works (I hand-recovered both ChatGPT DR and attach this session). IMPACT (production): blocks ALL real --attach Family consults (big packets >128KB can ONLY go via attach, not inline) + ALL ChatGPT fetch modes (DR/web_search require the tools menu). Root-cause direction (SIMPLIFY, per THE RULE #4 'post-click dropdown reads MUST use menu_snapshot()'): the tools/+ menu renders in a React portal OUTSIDE the document subtree — the tools-menu read must use menu_snapshot()/app-root scope (not document snapshot()), OR use the correct portal-scoped locator, so tool_* elements resolve post-click. Do NOT add a coord-click fallback in the driver (blind-coord banned); fix the snapshot scope so the mapped exact-match resolves. [priority: 16] [owner: taeys-hands-codex] [depends: w2c-deadsession-validate] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: w2e-tools-menu-detach-validate - Production-validate: a REAL ChatGPT consult that (a) uses --attach uploads the file (chip renders, screenshot-confirmed) and (b) selects a tools-menu mode (e.g. Deep research) via the engine (no manual coord-click) — both resolve the tool_* element from the (now correctly-scoped) menu snapshot and proceed to send+complete+extract. Evidence = production_observation obs-only + screenshot, NO commit_sha. NO TESTS. [priority: 17] [owner: taeys-hands] [depends: w2e-tools-menu-detach]

## Phase: p3-control - Conductor verify + merge [order: 3]

### Task: conductor-merge - Conductor independently verifies each production-validation verdict and merges each fix at its validated SHA (validated-tree==merge-tree), small separate commits per defect-cluster. Closes the audit. [priority: 30] [owner: conductor] [depends: w1a-dupsend-validate, w1b-gentimeout-validate, w1c-wrongmodel-validate, w1d-driftguard-validate, w1e-deliveryack-validate, w2a-settle-loader-validate, w2b-copybutton-gate-validate, w2c-deadsession-validate, w2d-claude-blindcoord-validate, w2e-tools-menu-detach-validate]

## Phase: p4-infra - Navigate settle race (orthogonal infra, non-blocking; conductor-flagged 2026-07-02) [order: 4]

### Task: nav-settle-fix - claude.py:420 navigate gate (runtime.navigate verify_change) fires its url-verify BEFORE the AT-SPI tree populates on a fresh claude.ai/new load -> empty snapshot -> url None -> navigate ok=False. Observed twice 2026-07-02 during w1c validation; self-heals when the tree is warm (direct probe raw_count=50 proves the tree is healthy once settled). NOT a w1c defect (9a0e083a diff does not touch navigate; a11y bus healthy, guid-matches). Root-cause shape (SIMPLIFY): navigate must settle-wait (bounded) for a non-empty/stable tree before url-verify, never race an empty tree. runtime.navigate / claude.py navigate gate. [priority: 40] [owner: taeys-hands-codex] [ref: CONSULT_ENGINE_AUDIT_2026-07-02.md]

### Task: nav-settle-validate - Production-validate: repeated real claude consults navigate cleanly (no url:None race) across cold and warm :3 states; normal consults unaffected. Evidence commit_sha-in-text + production_observation. NO TESTS. [priority: 41] [owner: taeys-hands] [depends: nav-settle-fix]

## User Stop Conditions
- stop_when_all_ready_tasks_dispatched
